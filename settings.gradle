rootProject.name = 'cplace-examples'

include ':cf.cplace.examples.spring'
include 'cf.cplace.examples.oauth2client'

if (!hasProperty('noIncludes') || !getProperty('noIncludes')) {
    includeBuild('../main')
} else {
    println "No includes of external gradle projects"
}
